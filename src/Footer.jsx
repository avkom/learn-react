import React from 'react';
import Logo from './Logo'

export default class Footer extends React.Component {
    render() {
        return (
            <footer>
                <Logo />
            </footer>
        );
    }
}